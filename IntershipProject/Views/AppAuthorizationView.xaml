<Page x:Class="IntershipProject.Views.AppAuthorizationView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
      xmlns:vm ="clr-namespace:IntershipProject.ViewModels"
      xmlns:local="clr-namespace:IntershipProject.Views"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="AppAuthorization">
    
    <Page.DataContext>
        <vm:AppAuthorizationViewModel/>
    </Page.DataContext>

    <Grid Background="#2f3748">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>


        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <Label  Height="40" HorizontalAlignment="Center" Content="Login" FontSize="20" Foreground="#17a2b8" FontFamily="Segoe UI"/>
            <TextBox Margin="0" Height="40" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Text="{Binding Login}"/>
            <Label  HorizontalAlignment="Center" Content="Password" Height="40" FontSize="20" Foreground="#17a2b8" FontFamily="Segoe UI"/>
            <PasswordBox Name="txtPassword" Margin="0" Height="40" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>

            <Button Content="Enter" FontSize="18" Margin="20" FontFamily="Segoe UI" Height="40" Command="{Binding LogInTheApp}" CommandParameter="{Binding ElementName=txtPassword}">
                <Button.Style>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Grid Background="#17a2b8" x:Name="Grid">
                                        <ContentPresenter Height="20" HorizontalAlignment="Center"/>
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" TargetName="Grid" Value="#FF3780A2"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontFamily" Value="MV Boli"/>
                        <Setter Property="FontSize" Value="16"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#FFEEAD22"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Label Content="{Binding ConnectionErrorStringContent}" Margin="0" Foreground="Red" HorizontalAlignment="Center" Visibility="{Binding ConnectionErrorStringVisibility}"/>
            <Controls:ProgressRing IsActive="{Binding IsConnectionChecking}" Foreground="#17a2b8" />
        </StackPanel>
    </Grid>
</Page>
